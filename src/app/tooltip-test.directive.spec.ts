import { TooltipTestDirective } from './tooltip-test.directive';

describe('TooltipTestDirective', () => {
  it('should create an instance', () => {
    const directive = new TooltipTestDirective();
    expect(directive).toBeTruthy();
  });
});
